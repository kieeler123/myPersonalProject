doctype html
html
head
title #{title} | #{video.name}
link(rel="stylesheet", href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.6.0/css/all.min.css")
link(rel="stylesheet" href="/style.css")
body
    div.main-title
        a(href="/")
            i.fa-brands.fa-youtube 
        h1= video.name
    .video-container
        iframe(
        width="100%",
        height="700px",
        src=video.video.includes("youtu.be") ? video.video.replace("youtu.be/", "www.youtube.com/embed/") : video.video.replace("watch?v=", "embed/"),
        frameborder="0",
        allowfullscreen
        )
    .video-info
        h3.video-title(style="justify-content: center; font-size: 20px")= video.name

    a(href="/") ← 홈으로 돌아가기