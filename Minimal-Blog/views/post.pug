extends layout
block content
    form(method="POST" action=`/delete/${title}`)
        h1= title
        div.content-box #{content}
        a.button(href="/") 돌아가기
        //- 수정/삭제 버튼 추가 - views/post.pug 내 block content 부분에 다음 줄 추가
        a.button(href=`/edit/${title}`) 수정하기
        form(method="POST" action=`/delete/${title}`)
            button(type="submit" style="margin-top: 10px; background-color: #dc3545;") 삭제하기